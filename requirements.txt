# ================================================================
# 과제 지정 환경: Python 3.10.12 / torch 2.5.1
# 버전 전부 고정 — Colab T4 기준 검증된 조합
# transformers==4.47.1: extra_special_tokens 버그 4.47.0에서 공식 수정
# tokenizers==0.21.0  : Qwen2.5 tokenizer.json 포맷 호환 (0.19는 파싱 불가)
# sentence-transformers==3.0.1: 3.4+의 torchcodec 충돌 방지
# ================================================================

# Core ML
torch==2.5.1

# Transformers / 양자화 — 버전 고정 필수
transformers==4.47.1
tokenizers==0.21.0
accelerate==0.34.2
bitsandbytes==0.49.2
datasets==2.21.0

# 질문 분류기 (Task 1)
scikit-learn==1.5.1

# RAG / Vector DB
langchain==0.2.16
langchain-community==0.2.17
chromadb==0.5.5
sentence-transformers==3.0.1
rank-bm25==0.2.2

# 형태소 분석
kiwipiepy==0.17.1

# Data Collection
requests==2.32.3
beautifulsoup4==4.12.3
selenium==4.24.0
webdriver-manager==4.0.2
lxml==5.3.0
pdfplumber==0.11.4
pymupdf==1.24.10
pymupdf4llm==0.0.17

# API & UI
gradio==4.42.0
fastapi==0.112.2
uvicorn==0.30.6
pydantic==2.8.2

# Utils
pandas==2.2.2
numpy==1.26.4
tqdm==4.66.5
python-dotenv==1.0.1
