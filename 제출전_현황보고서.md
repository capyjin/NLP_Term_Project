# 제출 전 현황 보고서

검토일: 2026-06-11  
기준: PROFESSOR_SUBMISSION_REVIEW.md, 실제 코드/파일 상태

---

## 1. PROFESSOR_SUBMISSION_REVIEW.md 항목별 개선 상태

### CRITICAL (제출 차단급)

| # | 지적사항 | 현재 상태 | 판정 |
|---|---------|-----------|:----:|
| 1 | 분류기 노트북 경로 수동 수정 필요 | 자동 루트 탐색(PROJECT_OVERRIDE + find_project_root) 구현됨 | **✅ 해결** |
| 2 | chat_output.json이 MOCK | 여전히 5건 전부 `[MOCK]` | **❌ 미해결** |
| 3 | 발표자료/UI영상/ZIP 없음 | 확인되지 않음 | **❌ 미해결** |
| 4 | chatbot.sh 종단 실행 미확정 | 코드상 DB 자동 재구축 로직 추가됨. Colab clean-run 필요 | **⚠️ 일부 해결** |
| 5 | ChromaDB 불일치 (158 vs 205) | 로컬은 158, Colab Drive는 205로 이미 재구축됨. Colab에서 실행 시 정합 | **⚠️ Colab에서 해결됨** |

### HIGH

| # | 지적사항 | 현재 상태 | 판정 |
|---|---------|-----------|:----:|
| 1 | README 7B/3B 불일치 | Qwen2.5-3B 기본 명시로 수정됨 | **✅ 해결** |
| 2 | 분류기-라우터 미연결 | 여전히 키워드 라우터 사용 (분류기 모델 미연결) | **❌ 미해결** |
| 3 | 실시간 미래 날짜 오류 | 2026-12-31 여전히 존재 | **❌ 미해결** |
| 4 | 모델 전달 방식 불명확 | HuggingFace 링크는 있음, Drive 공유 안내 부족 | **⚠️ 일부 해결** |
| 5 | 검증셋 중복 (25/30) | 현재 0/30 | **✅ 해결** |

### MEDIUM

| # | 지적사항 | 현재 상태 | 판정 |
|---|---------|-----------|:----:|
| 1 | 노트북 혼재 | 4개 노트북 존재 (classifier, colab_main, colab_main_clean, notebooks/) | **❌ 미해결** |
| 2 | API 사용법 약함 | README에 API 예시 없음 | **❌ 미해결** |
| 3 | 테스트 부족 | verify_pipeline.py(31/0/5), 단위테스트(7/7) | **⚠️ 일부 해결** |
| 4 | API/CLI 응답 경로 다름 | 여전히 상이 | **❌ 미해결** |
| 5 | 보안 (share=True 등) | 여전히 기본 활성화 | **❌ 미해결** |

---

## 2. 현재 프로젝트 주요 메트릭

```
파일/데이터        상태
──────────────────────────────────────
chunks.json        205건 ✅
train.json         194건
valid.json         30건
train/valid 중복   0건 ✅
chroma_db (로컬)   158건 (재구축 필요)
chroma_db (Drive)  205건 ✅ (Colab에서 재구축됨)

cls_output.json    5건, question/label OK ✅
chat_output.json   5건, MOCK=5 ❌
realtime_output.json 4건, 미래날짜 포함 ⚠️

문법 검사          PASS (compileall) ✅
Git 상태           main, origin과 동기 ✅
```

---

## 3. 제출 전 해야 할 일 (Step by Step)

### Step 1 — Colab에서 DB 재구축 + chatbot.sh clean-run
```
colab_main_clean.ipynb 열기
셀 A (패키지 설치) → 런타임 재시작 → 셀 0~6
```
완료 조건:
- [ ] chroma_db 205건
- [ ] chat_output.json MOCK 0건
- [ ] realtime_output.json 미래날짜 없음
- [ ] UI 정상 동작 확인

### Step 2 — Colab에서 classifier clean-run
```
classifier_박연진.ipynb 열기
셀 1 → 런타임 재시작 → 셀 2~8
```
완료 조건:
- [ ] macro F1 출력 캡처
- [ ] cls_output.json question/label OK

### Step 3 — 발표자료 + UI 영상
- [ ] 5분 발표자료 (구현 방법, Task1/Task2, RAG, UI)
- [ ] 2분 UI 영상 (질문→답변→대화흐름)
- [ ] 모델 다운로드 링크 문서

### Step 4 — 최종 ZIP 패키징
- [ ] Termproject_박연진.zip 생성
- [ ] 불필요 파일 제외 (내부 리뷰/캐시/에이전트/로그)

---

## 4. 불필요 파일 목록 (제출 ZIP에서 제외)

### 제외해야 할 파일들 (Pre-deletion 체크 필요)

| 파일/디렉토리 | 제외 이유 | 삭제 전 확인할 것 |
|--------------|---------|------------------|
| `.git/` | 개인 버전관리 | 모든 커밋이 push 됐는지 확인 |
| `.omc/`, `.omx/`, `.claude/` | 에이전트/세션 내부 상태 | 필요한 리뷰 MD는 미리 백업 |
| `.vscode/` | 개인 IDE 설정 | 없음 |
| `__pycache__/` | 실행 캐시 | 없음 (자동 생성) |
| `CTemppdf_extract.txt` | 깨진 임시 파일 | 내용 중요하면 발표자료에 옮김 |
| `bm25_validation.txt` | 개발 검증 로그 | BM25 결과는 발표자료에 요약 |
| `rag_bm25.txt` | 개발 검증 로그 | 필요 없음 |
| `rag_verify.txt` | 개발 검증 로그 | 필요 없음 |
| `retrieval_eval.txt` | 개발 검증 로그 | Top-1/3 정확도는 발표자료에 사용 가능 |
| `meal_test*.txt` | 개발 테스트 산출물 | 필요 없음 |
| `routing_test*.txt` | 개발 테스트 산출물 | 필요 없음 |
| `scholar*.txt` | 개발 테스트 산출물 | 필요 없음 |
| `check_result.json` | 개발 검증 결과 | 필요 없음 |
| `PROJECT_REVIEW.md` | 내부 리뷰 | 제출물에 불필요 |
| `PROJECT_STATE*.md` | 내부 상태 기록 | 제출물에 불필요 |
| `NEXT_AGENT_HANDOFF.md` | 내부 에이전트 기록 | 제출물에 불필요 |
| `답장.md` | 내부 의사소통 | 제출물에 불필요 |
| `PROFESSOR_SUBMISSION_REVIEW.md` | 사전 검토 문서 | 본인 참고용 |
| `최종정리.md` | 내부 점검 문서 | 제출물에 불필요 |
| `프롬프트.md` | Claude 전달용 프롬프트 | 제출물에 절대 불필요 |
| `2026_자연어처리_term_project.pdf` | 과제 안내 PDF | 발표자료와 혼동 방지용 제외 |
| `2_term_project_QA.pdf` | 과제 Q&A PDF | 발표자료와 혼동 방지용 제외 |
| `data/*_backup.json` | 백업 데이터 | 원본은 보존, ZIP에서만 제외 |
| `data/fake_test_chat.json` | 가짜 테스트 데이터 | 평가용 아님, ZIP 제외 |
| `notebooks/colab_main.ipynb` | 중복 노트북 (root에도 있음) | root colab_main_clean.ipynb만 유지 |
| 다양한 `.txt` 검증 파일들 | 일회성 테스트 출력 | 필요 없음 |

### 보존해야 할 파일들

```
classifier_박연진.ipynb   (필수: Task1)
chatbot.sh               (필수: Task2 평가 진입점)
chatbot.bat              (Windows 보조)
README.md                (필수: 실행 안내)
requirements.txt         (필수: 의존성)
src/                     (필수: 전체 소스)
scripts/inject_faq.py    (필수: FAQ 주입)
scripts/check_vector_db.py (권장: DB 검증)
data/                    (직접 구축 데이터 + 샘플 테스트)
outputs/                 (단, MOCK 없어야 함)
chroma_db/               (재구축 후 포함, 또는 자동 재구축 안내)
```

---

## 5. README에 추가/수정할 사항 (프롬프트 업데이트 제안)

현재 `README.md`는 다음 내용을 더 강조해야 함:

1. **권장 실행 환경 = Colab T4 GPU** 를 README 최상단에 bold로 명시
2. **Windows 로컬 실행은 보장하지 않음** 명시
3. **Task1 분류기와 Task2 라우터 관계 설명** 추가
   - "Task1은 klue/bert-base 기반 5분류기로 cls_output.json 생성
   - Task2는 실서비스 안정성을 위해 키워드 기반 라우터 + RAG 사용
   - 자세한 관계는 발표자료에서 설명"
4. **API 실행/요청 예시** 추가 (단, 과제 핵심은 chatbot.sh라고 구분)
5. **제출 전 검증 명령** 추가
   - `python scripts/check_vector_db.py`
   - `python -m compileall -q src scripts tests`
   - `outputs/chat_output.json` MOCK 0건 확인
6. **모델 다운로드 방법** 구체화
   - HuggingFace 자동 다운로드: 첫 실행 시 자동
   - Drive 공유 링크: professor에게 공유할 경우 권한 설정 안내

---

## 6. ChromaDB 관련 답변

> "chunk랑 db 205로 수 맞춰지지 않았나?"

**Colab Drive 기준으로는 이미 맞춰짐 (205/205).**  
`colab_main_clean.ipynb`의 셀 2 실행 결과:
```
청크 로드: 205건 (크롤링 158건 + FAQ 47건)
문서 205건 색인 완료
```

**로컬에 있는 chroma_db는 아직 158건** (Colab에서 재구축한 DB는 Drive에 저장됨).  
Colab에서 `chatbot.sh` 실행 시 `check_vector_db.py`가 자동으로 감지하고 `build_db.py --fresh`를 실행하므로 **걱정할 필요 없음**.

---

## 7. 프롬프트 개선 제안

현재 `프롬프트.md`의 Task 1~5는 잘 정리되어 있음.  
다음 사항 업데이트 권장:

1. **chroma_db 관련 문구 업데이트**  
   - "현재 chroma_db는 chunks.json과 불일치한다" → **Colab Drive 기준 205/205로 이미 정합**
   - 단, 로컬 chroma_db는 158이라 Colab 실행 시 자동 재구축됨

2. **colab_main_clean.ipynb 우선 실행 권장**  
   - COLAB_RUNBOOK.md 복붗보다 colab_main_clean.ipynb 자체를 열어 셀 A→0→2→4→5→6 순서 실행이 더 안전함

3. **유효한 출력 JSON 예시를 프롬프트에 포함**  
   - realtime_output.json의 미래날짜 문제 샘플을 구체적으로 명시

---

## 8. 결론

| 구분 | 상태 |
|------|------|
| 필수 구현 | ✅ 모두 완료 |
| CRITICAL 해결 | 2/5 (1,5 해결, 2,3,4 미해결) |
| HIGH 해결 | 2/5 (1,5 해결, 2,3,4 미해결) |
| MEDIUM 해결 | 0/5 |
| Colab 실행 준비 | ✅ chatbot.sh + DB 자동 복구 체계 완비 |
| 남은 핵심 | **Colab clean-run 1회 + 제출물 준비** |
